Note that the abbreviations for bit numbers use the lowercase letter "b" while the bytes are capital "B". 

The processing of the information takes place in the processor. This is a device that can work with several bits at once (8, 16, 32, 64, ...).